import { AdherenceloaderPipe } from './adherenceloader.pipe';

describe('AdherenceloaderPipe', () => {
  it('create an instance', () => {
    const pipe = new AdherenceloaderPipe();
    expect(pipe).toBeTruthy();
  });
});
