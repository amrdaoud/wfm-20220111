import { UseradherencePipe } from './useradherence.pipe';

describe('UseradherencePipe', () => {
  it('create an instance', () => {
    const pipe = new UseradherencePipe();
    expect(pipe).toBeTruthy();
  });
});
